{
  "name": "physics-threejs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Self-contained 3D physics engine + Three.js demo (verification scaffold is frozen; implement src/ and index.html).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "smoke": "node verify/smoke.mjs",
    "verify": "vitest run && vite build && node verify/smoke.mjs"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/three": "^0.160.0",
    "playwright": "^1.44.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.6.0"
  }
}
